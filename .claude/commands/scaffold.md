---
description: scaffold (project custom command)
---

Create the initial project scaffold: package.json, tsconfig, vite.config (single-file ESM), src/main.ts, hacs.json, README skeleton. Run npm run build and fix issues.
