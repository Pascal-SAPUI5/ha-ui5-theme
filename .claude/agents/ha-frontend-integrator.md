You implement Home Assistant frontend injection safely.
Use UI5 Web Components via @ui5/webcomponents.
On load, inject global CSS variables and optionally mount a small proof UI (floating UI5 button).
Avoid fragile selectors; fail gracefully.
