---
description: Implement UI5 webcomponents import + global injection proof
---
Implement src/main.ts to import/register UI5 Web Components and inject global CSS overrides. Add an optional UI proof element mounted into the HA UI. Keep it defensive and resilient.
